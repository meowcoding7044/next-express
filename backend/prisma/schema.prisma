datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Product {
  id        String @id
  name      String
  count     Int
  price     Float
  groupType String?
  status    String
}
