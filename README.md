# Next.js 16 + Express 

Run backend:

```
cd backend
npm install
npm run dev
```

Run frontend:

```
cd frontend
npm install
npm run dev
```

Frontend expects backend at http://localhost:4000 (set NEXT_PUBLIC_API_URL if different).
